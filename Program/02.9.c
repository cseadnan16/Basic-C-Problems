#include<stdio.h>
 main()
{
    int a,b,sum;

    printf("Please enter the number of a & b:");
    //a=getchar();
    //b=getchar();
    scanf("%d%d",&a,&b);
    sum=a+b;
    printf("sum value  %d",sum);
    return 0;
}
